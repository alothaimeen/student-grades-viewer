# 🎉 تحديث نظام التواريخ الهجرية

## ✨ ما الجديد؟

تم إضافة **مكتبة تقويم أم القرى** الخاصة بك والتي تحول التواريخ تلقائياً من ميلادي إلى هجري بصيغة عربية جميلة!

## 📋 التغييرات

### 1. ملف جديد: `umm_alqura_calendar.py`

مكتبة Python شاملة لتحويل التواريخ:
- ✅ تحويل من ميلادي إلى هجري (تقويم أم القرى)
- ✅ تنسيق بصيغة عربية جميلة
- ✅ دعم أسماء الأيام والشهور
- ✅ قابل لإعادة الاستخدام في جميع مشاريعك

### 2. تحديث `convert_notes_to_json.py`

البرنامج الآن يحول التواريخ **تلقائياً** من Excel إلى هجري بالصيغة:
```
الخميس, 15 جمادى الأولى, 1447
```

بدلاً من الصيغة الميلادية القديمة:
```
2025-11-06
```

### 3. تحديث `requirements.txt`

تمت إضافة المكتبة المطلوبة:
```
hijridate>=2.3.0
```

## 🚀 طريقة الاستخدام

### الطريقة الأولى: تحديث المكتبات فقط

إذا كان لديك Python و pip مثبتين:

```bash
pip install -r requirements.txt
```

### الطريقة الثانية: استخدام السكريبتات الجاهزة

السكريبتات الموجودة (`update_notes.ps1`, `update_notes.bat`) ستعمل كالعادة ولكن مع ميزة جديدة:
- التواريخ ستظهر بالهجري تلقائياً!

## 📊 أمثلة على التحويل

| التاريخ الميلادي | التاريخ الهجري (الصيغة الجديدة) |
|------------------|-----------------------------------|
| 2025-11-01 | السبت, 10 جمادى الأولى, 1447 |
| 2025-11-05 | الأربعاء, 14 جمادى الأولى, 1447 |
| 2025-11-07 | الجمعة, 16 جمادى الأولى, 1447 |

## 🎯 الميزة الأساسية

**لا حاجة لتعديل ملف Excel!**
- اترك التواريخ كأرقام في Excel
- البرنامج سيحولها تلقائياً عند القراءة
- ستظهر بالهجري في ملف JSON والموقع

## 💡 استخدام المكتبة في مشاريع أخرى

يمكنك استخدام `umm_alqura_calendar.py` في أي مشروع آخر:

```python
from umm_alqura_calendar import convert_excel_date_to_hijri, gregorian_to_hijri

# مثال 1: تحويل تاريخ من Excel
import pandas as pd
df = pd.read_excel('ملف.xlsx')
df['التاريخ'] = df['التاريخ'].apply(convert_excel_date_to_hijri)

# مثال 2: تحويل تاريخ محدد
hijri = gregorian_to_hijri(2025, 11, 6)
print(f"{hijri.day}/{hijri.month}/{hijri.year}")  # 15/5/1447
```

## 📚 التوثيق الكامل

راجع ملف `UMM_ALQURA_GUIDE.md` للتوثيق الشامل والأمثلة.

## 🧪 الاختبار

لاختبار المكتبة:

```bash
python umm_alqura_calendar.py
```

سيعرض أمثلة تحويل مختلفة.

## ⚙️ التقنيات المستخدمة

- **Python 3.x**
- **hijridate**: مكتبة تحويل التواريخ الهجرية (تستخدم خوارزمية تقويم أم القرى الرسمية)
- **pandas**: لقراءة ملفات Excel
- **openpyxl**: لدعم ملفات .xlsx

## 🎨 أنماط التنسيق المتاحة

المكتبة تدعم أنماط تنسيق مختلفة:

```python
# صيغة كاملة (افتراضية)
الخميس, 15 جمادى الأولى, 1447

# بدون اسم اليوم
15 جمادى الأولى, 1447

# بدون السنة
الخميس, 15 جمادى الأولى

# صيغة رقمية
15/5/1447
```

## ✅ الفوائد

1. **تلقائي 100%**: لا حاجة لنسخ ولصق التواريخ يدوياً
2. **دقيق**: يستخدم خوارزمية أم القرى الرسمية
3. **جميل**: عرض بصيغة عربية احترافية
4. **قابل للتوسع**: يمكن استخدامه في مشاريع أخرى
5. **يعمل للأبد**: يدعم أي تاريخ (ماضي أو مستقبل)

## 🔄 التحديثات المستقبلية

يمكن استخدام نفس المكتبة في:
- ✅ برنامج تحويل درجات الفترة 1 (`convert_excel_to_json.py`)
- ✅ برنامج تحويل درجات الفترة 2 (`convert_excel_to_json_period2.py`)
- ✅ أي برنامج آخر يحتاج تواريخ هجرية

فقط أضف هذا السطر في بداية البرنامج:
```python
from umm_alqura_calendar import convert_excel_date_to_hijri
```

## 📞 الدعم

للمزيد من المعلومات أو الاستفسارات، راجع:
- `UMM_ALQURA_GUIDE.md` - دليل شامل للمكتبة
- `umm_alqura_calendar.py` - الكود المصدري مع التوثيق

---

**تاريخ التحديث:** نوفمبر 2025  
**الإصدار:** 2.6 (إضافة دعم التقويم الهجري)
