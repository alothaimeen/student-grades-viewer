# 📚 تعليمات تحديث البيانات

## 🎯 الخطوات السريعة

### المرة الأولى فقط:

1. **تثبيت Python** (إذا لم يكن مثبتاً):
   - حمّل من: https://www.python.org/downloads/
   - تأكد من تفعيل خيار "Add Python to PATH" أثناء التثبيت

2. **تثبيت المكتبات المطلوبة**:
   ```bash
   pip install -r requirements.txt
   ```

---

## 🔄 تحديث البيانات

### الطريقة 1: باستخدام PowerShell (الأسهل) ⭐

1. عدّل ملف `الفترة 1.xlsx`
2. انقر مرتين على ملف `update.ps1`
3. انتهى! ✨

> **ملاحظة:** إذا لم يعمل، انقر بالزر الأيمن على `update.ps1` واختر "Run with PowerShell"

### الطريقة 2: من سطر الأوامر

1. افتح Terminal في مجلد المشروع
2. شغّل الأمر:
   ```bash
   python convert_excel_to_json.py
   ```

---

## 📋 متطلبات ملف Excel

تأكد من أن ملف `الفترة 1.xlsx` يحتوي على الأعمدة التالية:

- **الرقم التسلسلي**
- **الهوية**
- **الصف**
- **الطالب**
- **مجموع الغياب**
- **الواجبات**
- **أنشطة**
- **تطبيقات صفية**
- **المشاركة**
- **الاختبار التحريري**
- **الشفوي**
- **المجموع**

---

## ⚠️ حل المشاكل الشائعة

### المشكلة: "Python غير معرف"
**الحل:** تأكد من تثبيت Python وإضافته إلى PATH

### المشكلة: "الملف 'الفترة 1.xlsx' غير موجود"
**الحل:** تأكد من وجود ملف Excel في نفس المجلد مع اسم صحيح

### المشكلة: خطأ في المكتبات
**الحل:** شغّل الأمر:
```bash
pip install --upgrade pandas openpyxl
```

---

## 🎉 بعد التحديث

بعد تشغيل السكريبت بنجاح:
1. سيتم تحديث ملف `period1.json` تلقائياً
2. افتح الموقع `index.html` لرؤية البيانات المحدثة
3. جرّب البحث عن أحد الطلاب للتأكد

---

## 📞 دعم إضافي

إذا واجهت أي مشكلة، تواصل مع المطور أو افتح Issue على GitHub.

---

✨ **نصيحة:** يمكنك إنشاء اختصار لملف `update.ps1` على سطح المكتب لتسهيل الوصول!
