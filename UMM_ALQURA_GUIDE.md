# 📅 دليل مكتبة تقويم أم القرى

## نظرة عامة

مكتبة Python شاملة للتعامل مع التقويم الهجري (تقويم أم القرى) في جميع مشاريعك.

## ✨ المميزات

- ✅ تحويل من ميلادي إلى هجري والعكس
- ✅ تنسيق التواريخ بصيغ عربية جميلة
- ✅ دعم أسماء الأيام والشهور بالعربية
- ✅ استخدام خوارزمية تقويم أم القرى الرسمية
- ✅ معالجة تلقائية لتواريخ Excel
- ✅ قابل لإعادة الاستخدام في جميع المشاريع

## 📦 التثبيت

```bash
pip install hijridate
```

أو من ملف `requirements.txt`:

```bash
pip install -r requirements.txt
```

## 🚀 الاستخدام

### مثال أساسي

```python
from umm_alqura_calendar import format_hijri_date, gregorian_to_hijri

# تحويل تاريخ ميلادي إلى هجري
hijri_date = gregorian_to_hijri(2025, 11, 6)

# تنسيق التاريخ بصيغة عربية جميلة
formatted = format_hijri_date(hijri_date)
print(formatted)
# النتيجة: "الخميس, 15 جمادى الأولى, 1447"
```

### مع ملفات Excel

```python
from umm_alqura_calendar import convert_excel_date_to_hijri
import pandas as pd

# قراءة ملف Excel
df = pd.read_excel('البيانات.xlsx')

# تحويل عمود التاريخ إلى هجري
df['التاريخ'] = df['التاريخ'].apply(convert_excel_date_to_hijri)
```

## 📚 الدوال المتاحة

### `gregorian_to_hijri(year, month, day)`

تحويل تاريخ ميلادي إلى هجري.

**المعاملات:**
- `year`: السنة الميلادية
- `month`: الشهر الميلادي (1-12)
- `day`: اليوم (1-31)

**مثال:**
```python
hijri = gregorian_to_hijri(2025, 11, 6)
print(f"{hijri.day}/{hijri.month}/{hijri.year}")  # 15/5/1447
```

### `hijri_to_gregorian(year, month, day)`

تحويل تاريخ هجري إلى ميلادي.

**المعاملات:**
- `year`: السنة الهجرية
- `month`: الشهر الهجري (1-12)
- `day`: اليوم (1-30)

**مثال:**
```python
gregorian = hijri_to_gregorian(1447, 5, 15)
print(f"{gregorian.day}/{gregorian.month}/{gregorian.year}")  # 6/11/2025
```

### `format_hijri_date(hijri_date, include_day_name=True, include_year=True)`

تنسيق التاريخ الهجري بصيغة عربية جميلة.

**المعاملات:**
- `hijri_date`: كائن التاريخ الهجري
- `include_day_name`: إضافة اسم اليوم (افتراضي: True)
- `include_year`: إضافة السنة (افتراضي: True)

**أمثلة:**
```python
hijri = gregorian_to_hijri(2025, 11, 6)

# صيغة كاملة
print(format_hijri_date(hijri))
# النتيجة: "الخميس, 15 جمادى الأولى, 1447"

# بدون اسم اليوم
print(format_hijri_date(hijri, include_day_name=False))
# النتيجة: "15 جمادى الأولى, 1447"

# بدون السنة
print(format_hijri_date(hijri, include_year=False))
# النتيجة: "الخميس, 15 جمادى الأولى"
```

### `convert_excel_date_to_hijri(excel_date, format_style="full")`

تحويل تاريخ من Excel إلى تاريخ هجري منسق.

**المعاملات:**
- `excel_date`: التاريخ من Excel (datetime أو string أو رقم)
- `format_style`: نمط التنسيق:
  - `"full"`: الصيغة الكاملة مع اسم اليوم والسنة (افتراضي)
  - `"short"`: بدون اسم اليوم
  - `"no_year"`: بدون السنة

**أمثلة:**
```python
from datetime import datetime

date = datetime(2025, 11, 6)

# صيغة كاملة
print(convert_excel_date_to_hijri(date))
# النتيجة: "الخميس, 15 جمادى الأولى, 1447"

# صيغة قصيرة
print(convert_excel_date_to_hijri(date, format_style="short"))
# النتيجة: "15 جمادى الأولى, 1447"

# بدون سنة
print(convert_excel_date_to_hijri(date, format_style="no_year"))
# النتيجة: "الخميس, 15 جمادى الأولى"
```

### `get_day_name(date_obj)`

الحصول على اسم اليوم بالعربية.

**مثال:**
```python
from datetime import datetime

date = datetime(2025, 11, 6)
print(get_day_name(date))  # الخميس
```

### `get_month_name(month_number)`

الحصول على اسم الشهر الهجري بالعربية.

**مثال:**
```python
print(get_month_name(5))  # جمادى الأولى
print(get_month_name(9))  # رمضان
```

### `format_hijri_date_simple(hijri_date)`

تنسيق التاريخ الهجري بصيغة رقمية بسيطة (DD/MM/YYYY).

**مثال:**
```python
hijri = gregorian_to_hijri(2025, 11, 6)
print(format_hijri_date_simple(hijri))  # 15/5/1447
```

## 📋 الأشهر الهجرية

1. محرم
2. صفر
3. ربيع الأول
4. ربيع الآخر
5. جمادى الأولى
6. جمادى الآخرة
7. رجب
8. شعبان
9. رمضان
10. شوال
11. ذو القعدة
12. ذو الحجة

## 📅 أيام الأسبوع

- الأحد
- الاثنين
- الثلاثاء
- الأربعاء
- الخميس
- الجمعة
- السبت

## 🧪 اختبار المكتبة

لاختبار المكتبة، شغّل الملف مباشرة:

```bash
python umm_alqura_calendar.py
```

سيعرض أمثلة واختبارات شاملة للدوال المختلفة.

## 💡 أمثلة عملية

### استخدام في برنامج تحويل الملاحظات

```python
import pandas as pd
from umm_alqura_calendar import convert_excel_date_to_hijri

# قراءة ملف Excel
df = pd.read_excel('الملاحظات.xlsx')

# تحويل عمود التاريخ إلى هجري
df['التاريخ'] = pd.to_datetime(df['التاريخ'], errors='coerce')
df['التاريخ'] = df['التاريخ'].apply(convert_excel_date_to_hijri)

# النتيجة: التواريخ ستظهر بصيغة "اليوم, التاريخ الشهر, السنة"
```

### إنشاء تقرير بتواريخ هجرية

```python
from datetime import datetime
from umm_alqura_calendar import format_hijri_date, gregorian_to_hijri

# الحصول على التاريخ الحالي
today = datetime.now()
hijri = gregorian_to_hijri(today.year, today.month, today.day)

# طباعة تقرير
print(f"التاريخ الميلادي: {today.strftime('%Y-%m-%d')}")
print(f"التاريخ الهجري: {format_hijri_date(hijri)}")
```

### مقارنة تواريخ

```python
from umm_alqura_calendar import gregorian_to_hijri

# تاريخان مختلفان
date1 = gregorian_to_hijri(2025, 1, 1)
date2 = gregorian_to_hijri(2025, 12, 31)

print(f"بداية السنة: {date1.day}/{date1.month}/{date1.year}")
print(f"نهاية السنة: {date2.day}/{date2.month}/{date2.year}")
```

## ⚙️ التكامل مع مشاريع أخرى

يمكنك استخدام هذه المكتبة في أي مشروع Python. ببساطة:

1. انسخ ملف `umm_alqura_calendar.py` إلى مشروعك
2. ثبت `hijridate` من requirements.txt
3. استورد الدوال التي تحتاجها

```python
from umm_alqura_calendar import convert_excel_date_to_hijri, format_hijri_date
```

## 🔗 روابط مفيدة

- [مكتبة hijridate على PyPI](https://pypi.org/project/hijridate/)
- [تقويم أم القرى - ويكيبيديا](https://ar.wikipedia.org/wiki/تقويم_أم_القرى)

## 📝 ملاحظات

- المكتبة تستخدم خوارزمية تقويم أم القرى الرسمية
- التحويل دقيق ومُختبَر
- يمكن استخدامها لأي تاريخ (ماضي أو مستقبل)
- معالجة تلقائية للقيم الفارغة والتواريخ الخاطئة

## 🎯 الاستخدام في المشاريع الحالية

هذه المكتبة مُستخدمة حالياً في:
- ✅ برنامج تحويل الملاحظات السلوكية (`convert_notes_to_json.py`)

يمكنك استخدامها في:
- 📊 برامج تحويل الدرجات
- 📅 تطبيقات إدارة الطلاب
- 📝 أنظمة التقارير
- 🔔 تطبيقات التذكيرات
- وأي مشروع آخر يحتاج تواريخ هجرية!

---

**تم إنشاء المكتبة بواسطة:** GitHub Copilot  
**التاريخ:** نوفمبر 2025  
**الإصدار:** 1.0
